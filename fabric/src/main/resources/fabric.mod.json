{
  "schemaVersion": 1,
  "id": "${MODID}",
  "name": "${NAME}",
  "version": "${version}",
  "environment": "*",
  "icon": "logo.png",
  "authors": [
    "${AUTHOR}"
  ],
  "license": "MIT",
  "contact": {
    "homepage": "${CURSE_HOMEPAGE}",
    "sources": "${GIT_REPO}",
    "issues": "${GIT_REPO}/issues"
  },
  "description": "${DESCRIPTION}",
  "entrypoints": {
    "main": ["com.blamejared.wac.WACFabric"]
  },
  "depends": {
    "fabricloader": ">=${FABRIC_LOADER}",
    "java": ">=${JAVA}",
    "minecraft": "${MINECRAFT}"
  },
  "custom": {},
  "accessWidener": "wac.accesswidener",
  "mixins": [
    {
      "config": "wac.mixins.json",
      "environment": "*"
    },
    {
      "config": "wac.fabric.mixins.json",
      "environment": "*"
    }
  ]
}